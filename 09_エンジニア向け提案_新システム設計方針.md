# 新システム設計方針 — エンジニア向け提案

**提案者: 中村大樹（ビジネスオーナー）**
**日付: 2026-02-27**
**次回会議で議論したい内容**

---

## この資料の目的

現在のシステムは密結合によりAI開発が困難な状態にある。
これを解消し、AIファースト開発に移行する。
**ただし全部作り直すのではなく、「頭脳」だけ新しく作り、「手足」は今のシステムをAPIで繋いで使い続ける。**
本資料はビジネスオーナーとしての構想と判断をまとめたもの。
**エンジニアの技術的な見解・指摘・代替案を求めます。**

---

## 1. 設計の核心：「頭脳だけ新しく作り、手足はAPIで繋ぐ」

**4つの架空の会社** が API で連携しているように設計する。
ただし **新規に作るのは「頭脳」であるプラットフォームだけ。**
物流（倉庫業務）とモール併売は旧システムをそのまま使い、APIアダプタで繋ぐ。
チャリボンは既にAPI連携済みなので調整のみ。

```
                 🆕 ここだけ新しく作る
                 ─────────────────────
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│  🏢 バリューブックスプラットフォーム（株）【新規構築】             │
│     ─ 頭脳。データとお金の流れを握る ─                            │
│                                                                  │
│  📚 書誌DB（300万冊）  📊 査定・価格エンジン  👤 ユーザー管理    │
│  🛒 valuebooks.jp      📖 ライブラリ          💰 ポイント(後日)   │
│                                                                  │
│  【公開API】                                                      │
│  書誌API / 査定API / 買取API / 在庫API / 注文API / ユーザーAPI    │
│                                                                  │
└────────┬──────────────┬─────────────────────────┬────────────────┘
         │              │                         │
    フルフィルメント  買取API経由            在庫・出荷API経由
      API経由          │                         │
         │              │                         │
         ▼              ▼                         ▼
                 📦 ここは旧システムを残してAPIで繋ぐ
                 ────────────────────────────────────
┌────────────────┐ ┌────────────┐ ┌────────────────────────────┐
│                │ │            │ │                            │
│  🏭 バリュー    │ │ 💚 チャリボン│ │  🏪 本併売（株）           │
│  物流（株）    │ │  （株）    │ │                            │
│                │ │            │ │  ＝ 旧システムの            │
│ 【旧システム   │ │ 【既にAPI   │ │    モール連携部分          │
│  残存】        │ │  連携済み】 │ │                            │
│                │ │            │ │  【旧システム残存】         │
│ 入荷→検品     │ │ 寄付型買取  │ │                            │
│ →棚入れ→保管  │ │ サイト     │ │  Amazon / 楽天 / Yahoo     │
│ →ピッキング   │ │            │ │  / メルカリ / ラクマ       │
│ →梱包→出荷   │ │ NPO連携    │ │  への出品・注文取込・      │
│                │ │            │ │  価格管理                  │
│ 倉庫:上田原    │ │ 将来:      │ │                            │
│    秋和       │ │ 第三者も   │ │  将来: 他社にも併売機能    │
│    埼玉(予定) │ │ 買取サイト │ │  を提供（超多品種併売の    │
│                │ │ を構築可能 │ │  SaaS化）                  │
│ ★ 作り直さない│ │            │ │                            │
│  APIアダプタ  │ │            │ │  ★ 作り直さない            │
│  だけ新規     │ │            │ │   APIアダプタだけ新規       │
│                │ │            │ │                            │
└────────────────┘ └────────────┘ └────────────────────────────┘
```

### 「Vaboo」について

**閉鎖する。** 停滞しており、このタイミングで整理する。
買取APIが完成した後、必要であれば新ブランドとして再出発できる。

---

## 2. この設計が解決すること

| 今の問題 | この設計でどう解決するか |
|---------|----------------------|
| DB密結合でAIがコードを書けない | **新DB（Single Source of Truth）をゼロから設計。** すべてのデータの「正」が1箇所に |
| 1箇所の変更が全体に波及する | **API契約が変わらなければ他に影響しない** |
| データが散在して整合性が取れない | **新DBが唯一の真実。** 旧システムは新DBのデータを読み書きするだけ |
| モール連携の複雑さを新システムに持ち込みたくない | **旧システムの画面・業務ロジックはそのまま残す。** ただしDBは新DBを参照 |
| 倉庫業務の作り直しはリスクが大きい | **倉庫の画面・オペレーションはそのまま。** 現場は何も変わらない |
| 全部作り直すと時間がかかりすぎる | **新規構築はプラットフォームだけ。** 開発対象が激減し、リリースが早まる |
| 将来の事業拡張が現在のアーキテクチャでは困難 | **APIプラットフォーム。** 新しいパートナー＝新しいAPIクライアントを追加するだけ |

---

## 3. 重要な設計判断

### 3-1. 新しく作るのはプラットフォームだけ

```
【新規構築】プラットフォーム（頭脳）
   ＝ 書誌DB、査定、販売、買取ロジック、ユーザー、ライブラリ、valuebooks.jp

【旧システム残存】物流（手足）
   ＝ 入荷、検品、棚入れ、ピッキング、梱包、出荷 → 倉庫現場は何も変えない

【旧システム残存】モール併売（手足）
   ＝ Amazon/楽天/Yahoo/メルカリ/ラクマ → 併売の複雑さは新システムに持ち込まない

【既存のまま】チャリボン
   ＝ 既にAPI連携済み。新システムのAPIに繋ぎ先を変えるだけ
```

### 3-2. DB戦略：新DBが唯一の真実（Single Source of Truth）

**これが最も重要な設計判断。**

旧システムの画面・業務ロジックは残すが、**データの「正」は新DBに一本化する。**
旧システムの古いDBは「ローカルキャッシュ」扱いとし、マスターデータは必ず新DBから読み書きする。

> **📊 BigQuery調査で分かったこと（2026-02-27 調査）**
>
> 既存DBの `m_shoshi`（書誌マスター）と `m_genpin`（現品マスター）は、
> **既に別テーブルとして分離**されている。
> つまり「書誌DB中心」の構想は、既存のデータモデルと整合的。ゼロからデータ設計する部分は想像より少ない可能性がある。
>
> 一方、テーブル数は非常に多く（`valuebooks_db` だけで大量のテーブルがある）、
> DB切り替えを一括で行うのは**リスクが大きい**。
> → **テーブル単位の段階的切り替え（Phase方式）が必須。**
> → **書誌マスター（m_shoshi）から始めるのが最もリスクが低く効果が大きい。**

```
┌──────────────────────────────────────────┐
│                                          │
│  🆕 新DB ＝ 唯一の真実（Single Source     │
│              of Truth）                  │
│                                          │
│  書誌マスタ（300万冊）                    │
│  顧客データ                              │
│  在庫データ（論理 + 物理ロケーション）     │
│  注文・買取データ                         │
│  価格・査定データ                         │
│                                          │
│  ★ すべてのデータの「正」がここにある      │
│  ★ きれいに設計された新しいスキーマ        │
│                                          │
└──────┬───────────────────┬───────────────┘
       │                   │
   読み書きAPI          読み書きAPI
       │                   │
┌──────┴──────────┐  ┌──────┴──────────────┐
│                │  │                    │
│ 旧物流システム  │  │ 旧モールシステム    │
│                │  │                    │
│ 画面・業務     │  │ 出品・取込         │
│ ロジックだけ   │  │ ロジックだけ       │
│ 残す          │  │ 残す              │
│                │  │                    │
│ ※自前の古いDB │  │ ※自前の古いDB     │
│  はキャッシュ  │  │  はキャッシュ      │
│  扱い         │  │  扱い             │
│                │  │                    │
└────────────────┘  └────────────────────┘
```

**これにより：**
- ✅ 「どっちのDBが正しいの？」という問題が起きない
- ✅ 新旧システム間のデータ不整合が構造的に防げる
- ✅ 古いDBの密結合は残るが、もう「正」ではないので影響が限定される
- ✅ 将来、旧システムを完全に捨てる時にデータ移行が不要（既に新DBに全部ある）

**旧システム側に必要な改修：**
- 旧システムがデータを読み書きする時に、自前のDBではなく新DBのAPIを経由するように変更
- 一気に全部変えなくても、テーブルごとに段階的に切り替え可能
- 旧システムの古いDBは、切り替え完了したテーブルから順次「読み取り専用のキャッシュ」に降格

### 3-3. プラットフォーム内部の構成

| | 初日（現実的） | 成長したら |
|---|---|---|
| **コード** | 1リポジトリ | 必要に応じてモジュール分離 |
| **DB** | PostgreSQL（ドメインごとにスキーマ分離） | 負荷に応じてDB分離 |
| **デプロイ** | 1つのアプリケーション | サービスごとに独立 |

### 3-4. 旧システムの段階的なDB切り替え

旧システムのDB参照先を、テーブル単位で段階的に新DBに切り替える。
**BigQuery調査の結果、書誌（m_shoshi）と現品（m_genpin）が既に分離されているため、
Phase 1 は特に実現可能性が高い。**

```
【Phase 1】書誌マスターから始める（最もリスクが低く、効果が大きい）
  ★ 既存の m_shoshi（書誌マスター）が既に独立テーブルとして存在
  ★ ここから新DBに移すのが最も自然な出発点
  
  旧システム → 書誌データ（m_shoshi）は新DB APIから取得
  旧システム → 顧客データ（customer）は新DB APIから取得
  旧システム → 在庫の引当・数量は新DB APIに問い合わせ

【Phase 2】物流オペレーションのデータを新DBへ
  ★ 現品テーブル（m_genpin）はカラム数が非常に多く複雑
  ★ Phase 1 で書誌マスターが安定してから着手

  旧システム → 入荷・検品結果を新DB APIに書き込み
  旧システム → ピッキング指示を新DB APIから取得
  旧システム → 出荷完了を新DB APIに報告

【Phase 3】モールのデータを新DBへ
  旧システム → 在庫同期は新DB APIの在庫数を使用
  旧システム → モールで売れたら新DB APIに注文登録

  ★ 各Phaseが完了するごとに、旧DBの該当テーブルは「不要」になる
  ★ 最終的に旧DBは空になり、旧システムは「画面だけの箱」になる
```

**⚠️ エンジニアに確認したい最大のポイント：**
1. 旧システムのDB参照先を新DB（API経由）に段階的に切り替えることは技術的に可能か？
2. 密結合が激しすぎて「DB参照先を変えるくらいなら全部作り直した方が早い」なら、その判断を聞きたい
3. テーブル単位での段階的切り替えは現実的か？ それとも一括切り替えしかないか？

---

## 4. 各社が持つ API（モジュール間のインターフェース）

### 🏢 プラットフォーム（株）の API

| API | 主な操作 | 主な利用者 |
|-----|---------|-----------|
| **書誌API** | ISBN検索、書籍情報取得、書影URL取得 | 全社（全モジュール） |
| **査定API** | 買取価格の算出、販売適正価格の算出、市場価格の照会 | 買取モジュール、本併売、将来は外部（図書館・古書店） |
| **買取API** | 買取申込、査定結果返却、集荷手配、お支払い | チャリボン、将来の第三者パートナー |
| **在庫API** | 在庫照会、在庫引当（予約）、引当解除 | valuebooks.jp、本併売（旧システム） |
| **注文・出荷API** | 注文作成、出荷依頼、配送ステータス、追跡番号 | valuebooks.jp、本併売（旧システム） |
| **ユーザーAPI** | 認証、ユーザー情報、履歴照会 | valuebooks.jp、チャリボン |

### 🏭 バリュー物流（株）の API（旧システムにAPIアダプタを追加）

| API | 主な操作 | 主な利用者 | 備考 |
|-----|---------|-----------|------|
| **入荷API** | 入荷登録、検品結果報告 | プラットフォーム（買取） | 旧システムの入荷機能をAPI化 |
| **在庫管理API** | 棚入れ、ロケーション管理、倉庫間移動 | プラットフォーム（在庫） | 旧システムのWMS機能をAPI化 |
| **出荷API** | ピッキング指示、梱包、配送ラベル発行、出荷完了報告 | プラットフォーム（注文） | 旧システムの出荷機能をAPI化 |

---

## 5. プラットフォーム構想の将来性

この設計は**今のバリューブックスを作り直す**だけでなく、**プラットフォームとして拡張できる**構造になっている。

```
                   【今やること】                    【将来できること】

 買取API ──→  チャリボンが使う              →  第三者が独自の買取サイトを構築
                                                （NPO、企業、学校が自分のサイトで
                                                  VBの買取機能を使える）

 在庫API ──→  本併売(旧システム)が使う      →  第三者が独自の書店サイトを構築
              Amazonや楽天に出品               （Bookshop.org の進化版。
                                                  誰でもVBの在庫で書店を開ける）

 併売機能 ─→  VBの在庫をモールに出品        →  他社の古書店にも併売機能を提供
              Amazon/楽天/Yahoo/メルカリ        （超多品種×多販路の併売ノウハウと
              /ラクマの5販路                      システムをSaaSとして外販。
                                                  「本併売（株）」の事業化）

 査定API ──→  自社の査定で使う              →  図書館・古書店に有料提供
                                                （BetterWorldBooks モデル。
                                                  「この本いくら？」を API で提供）

 物流API ──→  自社の出荷で使う              →  他社の物流代行を受託
                                                （古本版 FBA。他の古書店の
                                                  在庫を預かって出荷する）
```

---

## 6. 機能の優先度（仕分け結果サマリー）

全80項目を仕分け済み。詳細は `07_機能仕分けシート.md` を参照。

### Step 1: プラットフォームの新規構築

**「頭脳」だけ新しく作る。倉庫業務・モール連携は旧システムのまま。**

| 領域 | 新規に作るもの | 旧システムが担当 |
|------|-------------|-----------------|
| 📚 書誌 | 書誌マスタ、書影（外部URL）、市場価格、コンディション | — |
| 📦 買取 | 申込受付、査定ロジック、結果通知、振込、集荷手配 | — |
| 📋 在庫 | 在庫引当、在庫数管理（論理的な在庫管理） | **倉庫管理（WMS）、棚入れ・ロケーション、現品管理 → 旧システム** |
| 🛒 販売 | 商品ページ、検索、カート、決済、送料（一律）、注文管理、会員管理 | — |
| 🚚 出荷 | 出荷指示（APIで物流に依頼）、出荷ステータス追跡 | **ピッキング・梱包・配送ラベル発行 → 旧システム** |
| 💰 決済 | クレカ決済、銀行振込 | — |
| 👤 顧客 | 顧客DB | — |
| 📱 サイト | valuebooks.jp | — |
| 📖 差別化 | **ライブラリ（初期から）** | — |
| 🏗️ 共通基盤 | 管理画面、通知基盤（メール）、データ移行 | — |
| 🔌 API連携 | **旧システムとの接続（物流API・モールAPI）** | 物流オペレーション、モール出品 |

**Step 1 に含めないもの:**
- ポイント経済圏 → Step 2 へ
- モール連携の作り直し → しない。旧システムをAPIで繋ぐ
- 倉庫業務の作り直し → しない。旧システムをAPIで繋ぐ
- バックヤード作業（入荷・バーコードスキャン・検品・棚入れ等）→ 旧システムのまま

### Step 2: 拡張

| 領域 | 含まれる機能 |
|------|-------------|
| 💰 ポイント | ポイント受取、ポイント利用、ポイント経済圏 |
| 🏪 モール | 旧システムのAPIアダプタ改善（Amazon/楽天/Yahoo/メルカリ/ラクマの5販路） |
| 🔌 買取API | チャリボン連携（API繋ぎ先変更）、将来の第三者連携 |
| 📊 分析 | BigQuery連携、売上ダッシュボード、GA4 |
| その他 | キャンペーン、新刊仕入れAI、エグゾテック連携 |

### Step 3: 付加価値

選書AI、本棚スキャン、プロアカウント、スマホアプリ（要判断）、クーポン等

### 将来: 物流の新規構築（必要に応じて）

旧システムの物流部分は、将来的にプラットフォームの成長や埼玉倉庫（エグゾテック）の稼働に合わせて、段階的に新システムに置き換える可能性がある。ただし **急がない。**

### 排除（D ランク）

- Vaboo（閉鎖）

---

## 7. エンジニアに聞きたいこと

### 最も重要な質問

| # | 質問 |
|---|------|
| **E0** | **旧システムのDB参照先を、新DB（API経由）に段階的に切り替えることは技術的に可能か？** テーブル単位で切り替えられるか、一括でしかできないか。密結合が激しすぎて「参照先を変えるくらいなら全部作り直した方が早い」なら、その判断を聞きたい |

### 技術的な質問

| # | 質問 |
|---|------|
| E1 | 新DB（Single Source of Truth）の設計で、ドメインごとにスキーマを分けた PostgreSQL で初日は十分か？ |
| E2 | 書誌DB（300万件）の検索を高速に実現するために、Elasticsearch 等が初日から必要か？ |
| E3 | この構成で AI（Cursor / Claude Code）が最も生産的にコードを書ける状態を作るために、何を準備すべきか？ |
| E4 | 旧DBから新DBへのデータ移行の工数とリスクをどう見積もるか？ **BQ調査で `m_shoshi`（書誌）と `m_genpin`（現品）は既に分離されていた。** `m_shoshi` → `customer` → `m_genpin` の順で段階的に移行するイメージだが、現実的か？ |
| E5 | 旧システムが新DBのAPIを経由してデータを読み書きする場合、パフォーマンス（レスポンス速度）は実用に耐えるか？ 倉庫のバーコードスキャン等、リアルタイム性が求められる場面で問題ないか？ |
| E6 | **既存BQパイプラインの鮮度問題**（データの遅延・欠損）は認識している。新システムのAPIからリアルタイムにBQへデータを流す設計にすれば解消されるか？ |

### スケジュールの質問

| # | 質問 |
|---|------|
| S1 | Step 1（プラットフォーム新規構築 + 旧システムのDB切り替えPhase1）のリリースまで、現実的にどれくらいかかるか？ |
| S2 | 開発チームの体制（人数・スキル）として何が必要か？ 旧システム側のDB切り替え開発は誰がやるか？ |
| S3 | 旧システムのDB切り替え（Phase 1→2→3）は、どのくらいの期間で段階的に進められるか？ |

### 設計への反論・代替案

| # | 質問 |
|---|------|
| D1 | この構想に対して、技術的に「それは難しい」「こっちの方がいい」というポイントはあるか？ |
| D2 | 新DBをSingle Source of Truthにするより、旧DBをそのまま使い続けてAPI層だけ挟む方が現実的か？ （BQ調査ではテーブル数が多く密結合が深いことが確認されている。段階的SSOT化と全面作り直し、どちらが工数的に有利か？） |
| D3 | 抜けている技術的な考慮事項はあるか？（セキュリティ、パフォーマンス、障害対策等） |

---

## 8. 判断の背景（なぜこうなったか）

この提案は、2026/2/26 の開発方針レビュー会議で議論した3案のうち **案3（フルリビルド）** を選択し、
その後ビジネスオーナーとして機能の仕分け・アーキテクチャ構想を行った結果です。

### 経営判断のポイント

1. **「頭脳」だけ新しく作り、「手足」の画面・業務ロジックは旧システムのまま。** 新規構築の範囲を最小化する
2. **DBはきれいに置き換える。** 新DBを唯一の真実（Single Source of Truth）とし、旧システムは新DBのAPIを通じてデータを読み書きする
3. **Amazon中心の設計から脱却する。** 書誌DBを中心に、自社サービスを最初に作る
4. **倉庫の画面・オペレーションは変えない。** 現場の混乱を防ぐ。ただしデータの「正」は新DB
5. **モール連携の画面・ロジックも変えない。** 5販路すべて維持。ただしデータの「正」は新DB
6. **チャリボンは既にAPI連携済み。** 繋ぎ先を変えるだけ
7. **本併売（株）の将来事業化を見据える。** 超多品種×多販路の併売ノウハウは、将来SaaS事業になり得る
8. **Vabooは閉鎖する。** 停滞しているサービスを整理するタイミング
9. **将来のプラットフォーム化を見据えた設計にする。** API設計を最初から意識する
10. **既存DBの構造は構想と整合的。** BQ調査で `m_shoshi`（書誌）と `m_genpin`（現品）が既に分離されていることを確認。「書誌DB中心」の構想はゼロからのデータ設計ではなく、既存構造を活かせる。ただしテーブル数が非常に多いため、DB切り替えは段階的に行う

### 参考資料

| ファイル | 内容 |
|---------|------|
| `07_機能仕分けシート.md` | 全80項目の機能仕分け詳細 |
| `08_新アーキテクチャ構想_書誌DB中心.md` | アーキテクチャ設計の詳細 |
| `06_経営判断_案3で進める方針.md` | 案3を選んだ経緯 |
| `03_会議まとめ_20260226.md` | 開発方針レビュー会議のまとめ |
| `04_その他/bigquery_valuebooks_ナレッジ.md` | 既存DBのBigQueryナレッジ（テーブル構造・パイプライン情報） |

---

**最終更新:** 2026-02-27
